.DS_Store
bld/
*.o
*.out
data/

# Local config (contains real api_key) — keep the .example version tracked
jiamiao.config.json

# Root-level cmake artifacts (in case someone runs `cmake .` from project root
# instead of the recommended `bld/` out-of-source build).
CMakeCache.txt
CMakeFiles/
CMakeScripts/
Makefile
cmake_install.cmake
CTestTestfile.cmake
compile_commands.json
Testing/
